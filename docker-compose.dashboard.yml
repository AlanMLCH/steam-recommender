services:
  dashboard:
    build: .
    image: game-rec-stage1:latest
    command: ["streamlit", "run", "src/dashboard/app.py"]
    ports:
      - "8501:8501"
    networks:
      - steam-rec-net

networks:
  steam-rec-net:
    external:
      name: steam-rec-net
