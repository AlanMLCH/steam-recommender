#!/usr/bin/env bash
# Fallback: simulate shadow by having gateway also POST to challenger asynchronously (ignore response)
echo "Gateway-level shadow should be implemented in code when needed (template uses Traefik)."